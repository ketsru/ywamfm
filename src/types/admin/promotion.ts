export interface Promotion {
    id: string
    activite_id: string

    nom: string
    specialite: string
    description: string
    
    is_active: boolean

    created_at: string
    updated_at: string
}
